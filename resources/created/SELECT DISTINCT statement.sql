-- SELECT DISTINCT Statement

USE test_db;

SELECT DISTINCT department_id
FROM employees;

/*
The DISTINCT clause allows us to get every category in a column
or in another words, allows us to get every value from a column
without any repetition
*/